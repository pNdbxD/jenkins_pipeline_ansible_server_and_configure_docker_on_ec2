---

# to group by tags in ansible inventory
# and then u can use those as hosts in playbooks
plugin: aws_ec2
regions:
  - ap-southeast-2
keyed_groups:
  - key: tags
    prefix: tagXXX


# to filter only some instances
#plugin: aws_ec2
#regions:
#  - ap-southeast-2
#filters:
#  instance-state-name: running
#  tag:Name: ans1-dev-ec2-1

# to dynamically use in ansible all instances in a region
#plugin: aws_ec2
#regions:
#  - ap-southeast-2